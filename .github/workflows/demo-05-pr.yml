name: Demo 05 - Build and Test Of PR

on:
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    name: Build and Test Of PR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build step
        run: echo "Building the application..."

      - name: Test step
        run: echo "Running tests... All tests passed!"

      - name: Report status
        run: echo "PR checks completed successfully!"
